<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tiksta.pro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden">
        <activity android:name="com.tiksta.pro.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.tiksta.pro.post.PostActivity" />
        <activity android:name="com.tiksta.pro.blog.BlogActivity" />
        <activity android:name="com.tiksta.pro.info.InfoActivity" />
        <activity android:name="com.tiksta.pro.blog.BlogPage"
            android:parentActivityName="com.tiksta.pro.blog.BlogActivity"
            android:theme="@style/AppTheme.SlidrActivityTheme"/>

    </application>

</manifest>